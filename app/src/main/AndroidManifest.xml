<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.android.work.demoset">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN"/>
    <uses-permission android:name="android.permission.BLUETOOTH"/>
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN"/>
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT"/>
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission  android:name = "android.permission.BIND_ACCESSIBILITY_SERVICE"
        tools:ignore="ProtectedPermissions" />

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DemoSet"
        tools:targetApi="31">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!--短链拉起-->
            <!--<intent-filter
                tools:ignore="AppLinkUrlError"
                android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data android:scheme="demoset" />
            </intent-filter>-->

        </activity>

        <activity android:name=".provider.ContentProviderTestActivity" />

        <activity android:name="com.android.work.demoset.plc.aidl.AidlDemoActivity" />

        <activity android:name=".plc.PLCDemoActivity" />

        <activity android:name=".plc.parcel.ParcelDemoActivity" />

        <activity android:name=".plc.messenger.MessengerActivity" />

        <activity android:name=".design.DesignPatternActivity" />

        <activity android:name=".hook.TestHookActivity" />

        <activity android:name=".databases.room.RoomActivity" />

        <activity android:name=".result.StartActivity1" />

        <activity android:name=".result.StartActivity2" />

        <activity android:name=".result.StartActivity3" />

        <activity android:name=".permission.PermissionActivity" />

        <activity android:name=".web.WebActivity" />

        <activity android:name=".displayCutout.DisplayCutoutActivity"
            android:theme="@style/a"/>

        <activity android:name=".dynamic_change_skin.ChangeSkinActivity"/>

        <activity android:name=".apt.AptActivity"/>

        <activity android:name="com.android.work.demoset.apt.route.ARouterActivity"/>

        <activity android:name=".deep.DeepLinkActivity"
            android:exported="true">
            <!--短链拉起-->
            <intent-filter
                tools:ignore="AppLinkUrlError"
                android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data android:scheme="demoset" />
            </intent-filter>
        </activity>

        <activity android:name=".ProxyActivity"/>

        <activity android:name=".accessibility.AccessibilityActivity"/>

        <service
            android:name=".plc.messenger.service.MessengerService"
            android:exported="true"
            android:process=":MessengerService" />

        <activity android:name=".hook.HookActivity"/>

        <provider
            android:authorities="com.android.work.demoset.provider.CustomContentProvide"
            android:name=".provider.CustomContentProvide"
            android:exported="true" />

        <!--        <provider-->
        <!--            android:authorities="com.android.work.demoset.fileProvider"-->
        <!--            android:name="androidx.core.content.FileProvider"/>-->


        <service android:name=".accessibility.AccessibilityService"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE"
            android:label="@string/accessibility_service_label"
            android:exported="true"
            tools:ignore="ExtraText">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>

            <meta-data
                android:name = "android.accessibilityservice"
                android:resource = "@xml/accessibility_service"  />
        </service>
    </application>


</manifest>